"""历史黄金案例动态注入（数据飞轮占位）。"""

from __future__ import annotations


def load_golden_cases() -> str:
    """从数据库加载历史高采纳案例，注入 Few-Shot。

    后续可替换为真实 DB 查询，例如：
    - 按学科/关键词检索被编辑复制次数最高的 Case
    - 格式化为 JSON 或自然语言示例
    """
    return ""
